// var Auth = require('../../../authorization');
// var tokens = Auth.tokens;

module.exports = function (req, res, next) {
    // tokens.get(req.query.accesskey, function(err, token){
        // if(err){
        	// if (err == "ER_INVALID_ACCESSKEY"){
            	// res.json({"err":"ER_NO_AUTH_OR_EXPIRED","ret":null});
        	// } else {
        		// res.json({"err":"ER_AUTH_FAILURE", "ret":null});
        	// }

        	// return;
        // }

        // add access token to middleware
        // req.scopes = {
        	// "access_token" : token
        // };

        // next();
    // });
};